version: "3.8"

services:
  # 1. The Laravel Application (PHP-FPM)
  app:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        user: laravel
        uid: 1000
    image: nkh-restaurant-app
    container_name: nkh_app
    restart: unless-stopped
    working_dir: /var/www
    volumes:
      - ./:/var/www
      - ./docker/php/local.ini:/usr/local/etc/php/conf.d/local.ini
    environment:
      APP_ENV: ${APP_ENV}
      APP_KEY: ${APP_KEY}
      APP_DEBUG: ${APP_DEBUG}
      DB_HOST: db
      DB_CONNECTION: pgsql
      DB_PORT: 5432
      DB_DATABASE: ${DB_DATABASE}
      DB_USERNAME: ${DB_USERNAME}
      DB_PASSWORD: ${DB_PASSWORD}
      REDIS_HOST: redis
    networks:
      - nkh_network
    depends_on:
      - db
      - redis

  # 2. The Web Server (Nginx)
  webserver:
    image: nginx:alpine
    container_name: nkh_webserver
    restart: unless-stopped
    ports:
      - "8000:80" # Exposing App on localhost:8000
    volumes:
      - ./:/var/www
      # This links the separate config file into the container
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf
    networks:
      - nkh_network
    depends_on:
      - app

  # 3. Database (PostgreSQL)
  db:
    image: postgres:16-alpine
    container_name: nkh_db
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${DB_DATABASE}
      POSTGRES_USER: ${DB_USERNAME}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      PGDATA: /var/lib/postgresql/data/pgdata
    volumes:
      - dbdata:/var/lib/postgresql/data
    networks:
      - nkh_network
    ports:
      - "5432:5432"

  # 4. Redis (Cache/Queue)
  redis:
    image: redis:alpine
    container_name: nkh_redis
    restart: unless-stopped
    networks:
      - nkh_network

networks:
  nkh_network:
    driver: bridge

volumes:
  dbdata:
    driver: local
